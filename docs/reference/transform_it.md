# Transform data

Transforms data into new values.

## Usage

``` r
transform_it(c_name, c_val, transformer)
```

## Arguments

- c_name:

  column (attribute) name.

- c_val:

  column value

- transformer:

  transformer function.

## Value

Returns transformed data.frame.
